我的文章集

```
git clone -b {name} {url} // clone 远程分支

git checkout -b {name} // 切换分支
git branch -a // 查看当前分支
git branch -d [branch name] // 删除分支
git branch -D [branch name] // 删除正在打开的分支
git push origin :[branch name] // 删除远程分支
```